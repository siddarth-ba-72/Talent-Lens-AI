spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://talent-lens-admin:talent-lens-admin@talent-lens-db-cluster.lc9vrlh.mongodb.net/?appName=talent-lens-db-cluster}
      database: ${MONGODB_DATABASE:talent-lens-db-cluster}
      auto-index-creation: false

app:
  ai-service:
    url: ${AI_SERVICE_URL:https://talentlens-ai-topaz.vercel.app}
    timeout-seconds: 30
    sourcing-timeout-seconds: 120
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}